name: Auto Rebase PRs (Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Disabled by policy
        run: echo "This workflow is intentionally disabled to prevent overwrite merges into main."
